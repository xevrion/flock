# WebSocket URL of the Flock server the demo connects to.
# Local dev defaults to ws://localhost:8787 if this is unset.
# For a deployed demo, point this at your wss:// server.
NEXT_PUBLIC_FLOCK_SERVER_URL=ws://localhost:8787
