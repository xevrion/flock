// Returns everyone currently present in the room except the local user,
// updating as people join and leave.

import type { PresenceUser } from "@flock-sdk/core";

export function usePresence(): PresenceUser[] {
  return [];
}
