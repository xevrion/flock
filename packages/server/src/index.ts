// Programmatic entry point for embedding the presence server inside an existing
// Node app. The CLI is a thin wrapper around this same FlockServer class.

export { FlockServer } from "./server.js";
export type { FlockServerOptions } from "./server.js";
